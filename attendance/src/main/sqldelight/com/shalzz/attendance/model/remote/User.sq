CREATE TABLE user (
    sapid TEXT PRIMARY KEY NOT NULL,
    name TEXT NOT NULL,
    course TEXT NOT NULL,
    password TEXT NOT NULL
);

select_all:
SELECT *
FROM user;

select_by_id:
SELECT *
FROM user
WHERE sapid = ? ;

select_count:
SELECT count(*)
FROM user;