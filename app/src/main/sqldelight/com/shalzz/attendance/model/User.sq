CREATE TABLE user (
    id TEXT PRIMARY KEY NOT NULL,
    roll_number TEXT NOT NULL,
    name TEXT NOT NULL,
    course TEXT NOT NULL,
    phone TEXT NOT NULL,
    email TEXT
);

select_all:
SELECT *
FROM user;

select_by_id:
SELECT *
FROM user
WHERE id = ? ;

select_count:
SELECT count(*)
FROM user;